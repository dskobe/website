---
title: "Unsere Sponsoren"
icon: "icons/sponsors.png"
description: ""
keywords: []
url:
  legacy: "/about-us-de/sponsors-de/?lang=de"
---

<div class="container-fluid p-0">
  <%= image_tag 'banners/2_header_thin.jpg', alt: data.config.site_name, class: 'img-fluid d-block w-100' %>
</div>

<div class="container mt-5 mb-5">
  <div class="row">
    <div class="col-12">
      <h1><%= current_page.data.title %></h1>

      <p>Die Deutsche Schule Kobe International würdigt die Unterstützung der folgenden Unternehmen/Organisationen. Deren Förderung und Spende hilft uns eine hochwertige Bildung zu vermitteln.</p>
      <p>Wenn Sie ebenfalls eine Patenschaft anbieten möchten, kontaktieren Sie bitte <%= mail_to 'marketing@dskobe.org', 'marketing@dskobe.org' %>.</p>

      <h2 class="mt-5">Gold Level</h2>
      <div class="container">
      <% data.sponsors.gold.each do |item| %>
        <div class="row">
          <div class="col-md-6 mt-5 pr-5">
            <p><%= link_to item.name, item.website, class: 'lead font-weight-bold' %></p>
            <p><%= item.abstract.en %></p>
          </div>
          <div class="col-md-6">
          <% link_to item.website do %>
            <%= image_tag item.image, alt: item.name, class: 'img-fluid d-block w-100' %>
          <% end %>
          </div>
        </div>
      <% end %>
      </div>

      <h2 class="mt-5">Bronze Level</h2>
      <div class="container">
        <div class="row">
        <% data.sponsors.bronze.each do |item| %>
          <div class="col-md-6">
            <p><%= link_to item.name, item.website, class: 'lead font-weight-bold' %></p>
            <p><%= item.abstract.en %></p>
          </div>
        <% end %>
        </div>
      </div>

      <h2 class="mt-5">Supporters</h2>
      <ul>
      <% data.sponsors.supporter.each do |item| %>
        <li><%= link_to item.name, item.website %></li>
      <% end %>
      </ul>

    </div>
  </div>
</div>
